
a= 3    # 변수 a에 3을 저장

if a== 2:  # a가 2와 같은지 비교함    
    print(A)   # False이므로 이 부분은 실행 되지 않음

if a==3 :  # a가 3과 같은지 비교
    print("B") # True 이므로 이 부분이 실행 됨.


if a==4:  # a와 4와 같은지 비교
    print("C")  # False이므로 이 부분은 실행되지 않음


else : # print("C") 대신  이 부분이 실행
    print("D")
